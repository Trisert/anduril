# --- Python --------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
*.egg
.installed.cfg
MANIFEST

# --- Virtualenvs ----------------------------------------------------------
.venv/
venv/
env/
ENV/

# --- Test artefacts ------------------------------------------------------
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
.mypy_cache/
.pyright_cache/
coverage.xml

# --- Editor / OS ---------------------------------------------------------
.idea/
.vscode/
.DS_Store
*.swp
*.swo
*~

# --- Nix / direnv --------------------------------------------------------
.direnv/

# --- Project state -------------------------------------------------------
# Real session / pasted-image / history dirs are picked up by XDG path
# logic and should never be checked in, but include the defaults here
# so a careless ``git add -A`` doesn't pick them up either.
.local/
*.log
